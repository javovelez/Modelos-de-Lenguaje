# Este repo es PÚBLICO: solo van los enunciados generados.
# Las fuentes .lab.md, las soluciones y las rúbricas viven en el repo
# privado que lo contiene (javovelez/Modelos-de-Lenguaje-fuente) y NO se publican.

# Datasets y corpus descargados al resolver los labs
data/
corpus/
*.parquet

# Vectores preentrenados: el lab de embeddings los baja del release
# `vectores-v1` de este mismo repo. El archivo pesa 81 MB y vive en el
# release, no en el arbol.
*.npz

# Modulos que los notebooks bajan del release `utils-v1` al ejecutarse.
lab1a_pipeline.py

# Jupyter
.ipynb_checkpoints/

# macOS
.DS_Store

# El corpus de los laboratorios (copia de MASSIVE es-ES). SE VERSIONA A
# PROPOSITO: los notebooks lo leen de aca para no depender del Hub en clase.
# Ojo: la regla data/ de arriba NO lo alcanza, y no hay que agregar datos/.
!datos/
